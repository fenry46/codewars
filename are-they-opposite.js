function isOpposite(s1, s2) {
    // if (!s1 || !s2 || s1.length !== s2.length || s1 === s2) return false;

    // for (let i = 0; i < s1.length; i++) {
    //     if (s1[i].toLowerCase() !== s2[i].toLowerCase()) {
    //         return false;
    //     }
    //     if (s1[i] === s2[i]) {
    //         return false;
    //     }
    // }
    let a = "";

    return a.toLowerCase()
}

console.log(isOpposite("aBcde", "AbCD"));